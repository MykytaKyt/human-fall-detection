numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.0
pyarrow>=14.0

# optional, only needed to re-run pose extraction on raw video (scripts/extract_features.py)
# ultralytics>=8.1

# dev/test
pytest>=7.0
matplotlib>=3.7
